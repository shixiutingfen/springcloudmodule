spring:
  application:
    name: elasticsearchmodule
  cloud:
    config:
      uri: http://localhost:8092/
      profile: dev

server:
  port: 8086

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/